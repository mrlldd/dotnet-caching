namespace mrlldd.Caching.Flags
{
    /// <summary>
    ///     The caching flag that represents a memory cache store.
    /// </summary>
    public sealed class InMemory : CachingFlag
    {
        private InMemory()
        {
        }
    }
}